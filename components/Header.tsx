"use client";import{useState}from"react";import{Menu,X}from"lucide-react";const links=[["Studio","#studio"],["Projects","#projects"],["Services","#services"],["Contact","#contact"]];export default function Header(){const[open,setOpen]=useState(false);return <header className="absolute left-0 top-0 z-50 w-full text-white"><div className="container-main flex h-24 items-center justify-between"><a href="#" className="font-display text-xl tracking-[0.18em]">ATELIER</a><nav className="hidden items-center gap-9 md:flex">{links.map(([l,h])=><a key={l} href={h} className="text-xs uppercase tracking-[0.2em] text-white/80 hover:text-white">{l}</a>)}</nav><button className="md:hidden" onClick={()=>setOpen(!open)} aria-label="Menu">{open?<X size={25}/>:<Menu size={25}/>}</button></div>{open&&<div className="absolute left-0 top-24 w-full border-t border-white/10 bg-black/95 px-7 py-8 md:hidden"><nav className="flex flex-col gap-7">{links.map(([l,h])=><a key={l} href={h} onClick={()=>setOpen(false)} className="text-sm uppercase tracking-[0.2em]">{l}</a>)}</nav></div>}</header>}