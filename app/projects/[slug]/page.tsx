import type {Metadata} from "next";import Image from "next/image";import {notFound} from "next/navigation";import {createServerSupabase} from "@/lib/supabase-server";
type P={title:string;slug:string;location:string|null;category:string|null;image_url:string;description:string|null};
async function getProject(slug:string){const s=await createServerSupabase();const{data}=await s.from("portfolio_projects").select("title,slug,location,category,image_url,description").eq("slug",slug).eq("published",true).maybeSingle();return data as P|null}
export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const{slug}=await params;const p=await getProject(slug);if(!p)return{};return{title:p.title,description:p.description||`${p.title} — project by ATELIER`,openGraph:{title:p.title,description:p.description||"Architecture & interior by ATELIER",images:[p.image_url]}}}
export default async function ProjectPage({params}:{params:Promise<{slug:string}>}){const{slug}=await params;const p=await getProject(slug);if(!p)notFound();return <main className="min-h-screen bg-[#f4f1eb]"><div className="container-main py-8"><a href="/#projects" className="text-xs uppercase tracking-[.18em]">← Back to projects</a><div className="mt-10 grid gap-10 md:grid-cols-[1.1fr_.9fr] md:items-end"><div><p className="text-xs uppercase tracking-[.25em] text-[#77736c]">{p.category} · {p.location}</p><h1 className="mt-4 font-display text-5xl leading-none md:text-8xl">{p.title}</h1></div><p className="text-sm leading-7 text-[#77736c]">{p.description}</p></div><div className="mt-12 overflow-hidden"><Image src={p.image_url} alt={p.title} width={1800} height={1200} priority sizes="100vw" className="max-h-[75vh] w-full object-cover"/></div></div></main>}
